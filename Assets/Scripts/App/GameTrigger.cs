// Semantic triggers fired by states to request transitions without knowing the destination.
public enum GameTrigger
{
    SceneValidated,
    BaseDestroyed,
    AllWavesCleared,
    RestartRequested
}
